# Runtime "database" — created on first use, never commit real contacts/users.
/data/*.json

# Generated public phonebook (regenerated whenever contacts change).
/phonebook.xml

# Editor / OS noise
.DS_Store
*.swp
